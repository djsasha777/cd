apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iotapp.fullname" . }}-test-connection"
  labels:
    {{- include "iotapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iotapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
