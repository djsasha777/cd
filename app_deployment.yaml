apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-iot-run
  namespace: iotnamespace
  labels:
    name: myappiotimage
    app: myappiotimage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myappiotimage
  template:
    metadata:
      labels:
        app: myappiotimage
    spec:
      containers:
      - name: myappiotimage
        image: djsasha777/iotimage:3.0
        ports:
        - containerPort: 8088
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: mongodb-secrets
              key: mongo-root-username
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongodb-secrets
              key: mongo-root-password
        - name: ME_CONFIG_MONGODB_SERVER
          valueFrom:
            configMapKeyRef:
              name: mongodb-configmap
              key: database_url
---
apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: iotnamespace
spec:
  selector:
    app: myappiotimage
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8088
      nodePort: 30888