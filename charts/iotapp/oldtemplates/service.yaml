apiVersion: v1
kind: Service
metadata:
  name: {{ include "iotapp.fullname" . }}
  labels:
    {{- include "iotapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iotapp.selectorLabels" . | nindent 4 }}
